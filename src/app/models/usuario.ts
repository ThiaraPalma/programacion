export interface Usuario {

    uid: string | any; // id para Firebase o auntentificacion
    nombre1: string; // -> nombre
    email: string;// -> email
    rol: string; // -> roles
    contrasena: string;
}
